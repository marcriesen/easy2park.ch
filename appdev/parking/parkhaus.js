$(document).ready(function () {
    'use strict';

    // Backlink hinzufügen
    var $link = $("<a/>", {href:'./index.html', text: 'zurücj zu den Parkplätzen'});
    $("body").prepend($link);

    var $parkhausliste = $('#parkhausliste');

    var modus = 'work';











});